macros:
  - name: cent_to_dollars
    description: A macro function that convert cents to dollars and round to the user input decimal places, or 2 if none provided
    arguments:
      - name: column_name
        type: string
        description: The name of the column you want to convert
        
      - name: decimals
        type: integer
        description: Number of decimal places, default is 2